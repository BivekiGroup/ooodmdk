---
import Page from "../requisites.astro";
import { normalizeLocale } from "../../i18n";

const { lang: langParam } = Astro.params;
const lang = normalizeLocale(langParam);
---

<Page lang={lang} />
